import express from "express";
// import {bookRide,cancelRide,history} from '../controllers/booking.js'

const router= express.Router();

// router.get('/',bookRide);
// router.delete('/',cancelRide);
// router.get('/history',history);

export default router;